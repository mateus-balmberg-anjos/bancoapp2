<resources>
    <!-- Tema principal do aplicativo -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryVariant">@color/orange</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <item name="android:windowMinWidthMajor">90%</item>
        <item name="android:windowMinWidthMinor">90%</item>
        <item name="android:padding">0dp</item>

        <item name="colorButtonNormal">@color/black</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <!-- Estilo para o diálogo com fundo arredondado e cor personalizada -->
    <style name="CustomDialogTheme" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:background">@drawable/dialog_background</item> <!-- Fundo cinza escuro -->
        <item name="android:textColorPrimary">@color/dialogText</item>
    </style>

    <!-- Estilo para os botões do diálogo -->
    <style name="DialogButtonStyle">
        <item name="android:backgroundTint">@color/buttonOrange</item> <!-- Fundo laranja para os botões -->
        <item name="android:textColor">@android:color/white</item>
        <item name="android:padding">10dp</item>
    </style>

    <!-- Estilo para a caixa de texto do diálogo (onde o usuário digita) -->
    <style name="EditTextDialogStyle">
        <item name="android:background">@color/gray</item> <!-- Cor de fundo cinza claro -->
        <item name="android:textColor">@color/black</item> <!-- Cor do texto preta -->
    </style>
</resources>
